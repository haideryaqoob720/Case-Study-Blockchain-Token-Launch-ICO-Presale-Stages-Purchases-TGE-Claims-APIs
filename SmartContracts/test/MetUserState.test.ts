/**
 * NOTE:
 * Cross-chain state tests removed for portfolio purposes.
 */
import { expect } from "chai"

describe.skip("MetUserState (portfolio demo — disabled)", () => {
    it("placeholder", () => {
        expect(true).to.equal(true)
    })
})
