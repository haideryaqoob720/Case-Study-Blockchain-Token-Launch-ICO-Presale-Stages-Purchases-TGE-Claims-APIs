/**
 * NOTE: Portfolio demo — script disabled.
 */
async function main() {
    console.log("[Portfolio demo] Script disabled — full tooling is private.")
}

main().catch((e) => {
    console.error(e)
    process.exit(1)
})
