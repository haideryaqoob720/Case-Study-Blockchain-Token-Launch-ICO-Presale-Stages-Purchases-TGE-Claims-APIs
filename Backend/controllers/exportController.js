/**
 * NOTE:
 * This part of the code is removed for portfolio purposes.
 * Full implementation is private.
 */

exports.exportData = async (_req, res) =>
  res.status(200).json({ success: true, message: 'Demo mode — feature removed' });
